# Yarn configuration for Pet Adoption Backend

# Save exact versions in package.json
save-exact true

# Automatically add the --production flag to yarn install when NODE_ENV is set to "production"
production-mode true

# Disable emoji in output (for CI/CD)
# emoji false

# Set network timeout
network-timeout 300000